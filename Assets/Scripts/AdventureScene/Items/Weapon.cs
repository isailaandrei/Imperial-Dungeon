using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour {

	/*public P1_MoveAnim owner;
	public float stillX;
	public float stillY;
	public SpriteRenderer sr;

	void Start () {
		owner = transform.parent.GetComponent<P1_MoveAnim> ();
		sr = transform.GetComponent<SpriteRenderer>();
		stillX = transform.position.x;
		stillY = transform.position.y;
	}
	
	// Update is called once per frame
	void Update () {
		switch (owner.move) {

		case Direction.Still:
			transform.position.Set(stillX, stillY, 0);
			sr.sortingOrder = 2;
			break;
		case Direction.Down:
			transform.position.Set (stillX, stillY, 0);
			sr.sortingOrder = 2;
			break;
		case Direction.Up:
			transform.position.Set (-stillX, stillY, 0);
			sr.sortingOrder = 1;
			break;
		case Direction.Left:
			
			break;
		case Direction.Right:
			
			break;
		case Direction.UpRight:
			
			break;
		case Direction.UpLeft:
			
			break;
		case Direction.DownRight:
			
			break;
		case Direction.DownLeft:
			
			break;
		case Direction.Dead:
			sr.sortingOrder = 0;
			break;
		}
	}*/
}
